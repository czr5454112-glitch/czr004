{
  "schema_version": "phase3_pibt_trace_v1",
  "description": "Raw PIBT event schema used to derive Phase3 directed edge labels.",
  "required_fields": [
    "schema_version",
    "run_id",
    "iteration",
    "event_index",
    "kind",
    "agent_id",
    "from_id",
    "to_id",
    "at_goal",
    "map",
    "scen",
    "agents",
    "seed"
  ],
  "kind_values": ["committed", "blocked"],
  "storage_policy": "Raw traces are large and belong under ignored artifacts/teacher/traces/ unless a small audit sample is explicitly force-added."
}

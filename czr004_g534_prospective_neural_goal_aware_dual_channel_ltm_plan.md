# Repair5G.5.34 Prospective Neural Goal-Aware Dual-Channel UpdateLTM Plan

This round audits G5.33, computes exact goal-distance diagnostics where local maps/scenarios permit, expands the bounded UpdateParams family, runs broader solver probes, trains actual Torch selector/ranker/safety models, and performs prospective heldout selected-rule replay.

Claims remain closed: `phase5p5_allowed=false`, `phase6_allowed=false`, `runtime_claim_allowed=false`, `learned_runtime_policy_validated=false`, and `aaai_ready=false`.

No edits are allowed under `external/lacam2/lacam2/**`, and candidate expansion uses only project-owned adapter grammar already present in `cpp/tools/phase1a_batch.cpp`.

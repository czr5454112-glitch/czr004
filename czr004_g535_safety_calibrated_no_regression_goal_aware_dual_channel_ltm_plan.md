# Repair5G.5.35 Safety-Calibrated No-Regression Goal-Aware Dual-Channel UpdateLTM Plan

This round continues after G5.34 commit `de6d211` and keeps all solver semantics unchanged.

## Objective

G5.34 found prospective selected-vs-additive improvement but also found four success regressions. G5.35 treats success preservation as the lexicographic first objective:

1. Verify and red-team G5.34 artifacts.
2. Recompute prospective evidence with hard success-regression penalties.
3. Autopsy every G5.34 success-regression context.
4. Build lexicographic safety labels and balanced risk/fallback training sets.
5. Train/evaluate a calibrated success-regression safety gate.
6. Train/evaluate a constrained selector with abstention/fallback.
7. Create and materialize a bounded no-regression prospective replay plan.
8. Analyze no-regression evidence and write an honest decision.

## Guardrails

- No edits under `external/lacam2/lacam2/**`.
- No changes to PIBT conflict semantics, candidate domains, agent actions, priorities, h-values, candidate deletion, high-level LaCAM* search, OPEN/EXPLORED/rewrite/incumbent pruning, or restart semantics.
- Do not inspect or run reserved IDs `166..205`.
- Keep `phase5p5_allowed=false`, `phase6_allowed=false`, `runtime_claim_allowed=false`, `learned_runtime_policy_validated=false`, and `aaai_ready=false` in all summaries.
- Do not claim runtime promotion, Phase5.5, Phase6, learned-runtime validation, or AAAI readiness.

## Local Execution Shape

The default replay is intentionally bounded for local execution: it includes every G5.34 prospective context and all known G5.34 success-regression contexts, then adds a small heldout seed slice outside the reserved range. The scripts keep the full planned seed range explicit so the same workflow can be expanded later without changing the analysis contract.

Raw solver logs, if generated by an expanded replay, stay under ignored `outputs/logs/phase5p5_repair5g535_*`. The committed artifacts are bounded reports, summaries, tables, and model manifests.

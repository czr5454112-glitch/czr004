# Repair5G.5.23 Full-Primary Teacher Repair Plan

Date: 2026-06-08

G5.23 treats G5.22 as mixed evidence: the targeted response-surface oracle
signal is useful, but the teacher/surrogate package is not promotable because
its `feature_*` namespace leaked outcome/oracle fields and it only covered a
targeted 21-context panel.

The execution package repairs that state without opening runtime claims:

1. Verify the G5.22 starting artifacts and keep the reserved-ID and closed-claim
   gates active.
2. Explain the G5.22 contradiction with tables: candidate-space oracle gain,
   surrogate failure, leakage audit, coverage limits, concentration, and
   controls.
3. Select a deterministic full-primary candidate set: old14 controls, retained
   G5.18 controls, and a capped/diverse/risk-aware subset of G5.22 response
   candidates.
4. Run a 60-context full-primary response-surface probe with budgets
   `1000,2000`, `max_workers=1`, observed IDs only, and no IDs `166..205`.
5. Analyze full-primary oracle behavior against static, additive, old14,
   retained G5.18, selected G5.22, mixed oracle, fixed-candidate, region-level,
   and avoidable-risk guarded baselines.
6. Mine static-recovery contexts and run a focused recovery probe only when the
   mined contexts are reliable and not already explained by G5.22 coverage.
7. Create leakage-free teacher v2 tables using runtime-safe `feature_*`
   namespaces and outcome/target/audit/oracle namespaces for labels.
8. Create runtime-safe trace feature matrices from available pre-choice
   map/agent/trace/candidate/geometry metadata.
9. Re-evaluate offline surrogates with heldout splits, bootstraps,
   calibration, source-blind/random/shuffled controls, and ablations.
10. Write a final decision with Phase5.5, Phase6, runtime, learned-runtime, and
    AAAI claims closed.

Closed claims remain:

```text
phase5p5_allowed=false
phase6_allowed=false
runtime_claim_allowed=false
learned_runtime_policy_validated=false
aaai_ready=false
```

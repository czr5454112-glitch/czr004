# Repair5G.5.61 Decision

Decision: `g561_goal_aware_actor_dev_replay_failed_continue_hard_negative_acquisition`

G5.60 is reclassified as a replay-materialization/identity contamination, not a clean direct-actor failure. The exact-materialized subset has zero success regressions, while 135 actor rows executed fallback additive settings. The valid scenario bank status is `g561_valid_scenario_bank_expanded_component_aware` with `2600` valid instances. The G5.61 materialization contract status is `g561_materialization_contract_passed` with `85` executed vectors. The replay ladder status is corrected=`g561_corrected_g560_scalar_replay_executed_exact_materialization` and architecture=`g561_architecture_replay_executed_exact_materialization`. The development replay status is `g561_development_replay_executed_exact_materialization` and hard-negative acquisition is `g561_hard_negative_acquisition_completed_with_success_regressions`. The primary seed training status is `g561_primary_seed_training_completed`. The final attribution status is `fine_tune_on_hard_negatives_and_rerun_frozen_development_panel` across data, representation, loss, materialization, and solver behavior. The next required work is fine-tune on acquired hard negatives and rerun one frozen-comparison development panel.

All Phase5.5, Phase6, runtime, learned-policy, and AAAI claims remain closed.

# G5.65 Bug And Assumption Ledger

This ledger records bugs, weak gates, and implementation assumptions found while finishing G5.65. Entries are part of the pushed evidence bundle so future debugging does not depend on external review only.

## Entries

| ID | Source | Finding | Impact | Fix / Follow-up | Status |
| --- | --- | --- | --- | --- | --- |
| G565-B001 | Plan audit / GPT Pro critique | G5.64 rich scaling was underpowered: small context bank, tiny train/validation split, one fold, few seeds, and weak loss reduction. | Offline rich-scaling claim could be noise. | Added `run_repair5g565_current_bank_scaling.py` for physical-map folds, multi-seed matched controls, nested sizes, and canonical repaired loss. | Addressed in code; needs full 5090 run. |
| G565-B002 | Plan audit / GPT Pro critique | G5.64 rich-vs-scalar comparison was anchor-confounded: rich residual initialized near G556 while scalar used full-bound sigmoid midpoint. | Apparent rich advantage/disadvantage was not a fair architecture comparison. | Added matched G556 residual heads in `matched_theta_heads.py`; B1/B2/E0/E1/E2 all start at G556. | Addressed and unit-tested. |
| G565-B003 | Plan audit / GPT Pro critique | Oracle and actor losses diverged: oracle used repaired floor/nearest/adaptive geometry while actor trained on the older set-valued loss. | Offline actor could optimize the wrong objective. | Added canonical `repaired_set_risk.py`; G565 training/evaluation uses the same loss config and writes config hash artifacts. | Addressed and unit-tested. |
| G565-B004 | Plan audit / GPT Pro critique | Oracle pass was partly trivial because many contexts were already optimal at G556. | Pass/fail gates could hide lack of usable learning opportunity. | Added `opportunity_labels.py` and loss ablation output with nontrivial/trivial/opportunity categories. | Addressed in code; needs full artifact review. |
| G565-B005 | Plan audit / GPT Pro critique | Neural memorization gate was too weak: only one seed and tiny context coverage. | Could miss representation or loss-learning failure. | Added strong memorization script for 1/4/8/16/32 context sizes, multi-seed larger sizes, architecture controls, and gap-closure tables. | Addressed in code; needs full 5090 run. |
| G565-B006 | Plan audit / GPT Pro critique | G5.64 solver-transfer panel reused older G5.62 rows instead of fresh G5.64/G5.65 actor exact materialization. | Solver-transfer claim was stale. | Added `run_repair5g565_fresh_solver_panel.py` to generate theta rows from G565 checkpoints and run the exact solver under the G562 materialization/audit contract. | Addressed in code; needs full 5090 exact run. |
| G565-B007 | Implementation audit | The first G565 implementation pass lacked explicit alpha-response, context-expansion, expanded-training, and final-decision scripts even though the plan listed them. | The pipeline could finish with missing required artifacts. | Added alpha response, conditional context expansion, expanded training, merge wrapper, decision writer, and required-artifact checks. | Addressed in code; needs smoke and full run. |
| G565-B008 | Implementation audit | Exact replay pair field names differ across G561/G562/G565 tables. | Merge/retrain scripts risk silently dropping useful rows or treating stale rows as fresh. | Added `exact_replay_dataset.py` with shared context/candidate/materialization parsing helpers; G565 decision requires fresh exact rows from the fresh panel. | Partially addressed; helper should be reused in any future replay scripts. |
| G565-B009 | Pre-remote audit | `write_repair5g565_decision.py` accepted memorization decisions named `supported/partial`, but `run_repair5g565_neural_memorization.py` emits `g565_neural_memorization_strong_pass` on success. | A valid strong memorization run would still be marked `g565_incomplete_evidence_continue`. | Updated the final decision gate to accept `g565_neural_memorization_strong_pass`; rerun py_compile/smoke before remote launch. | Addressed in code. |
| G565-B010 | Remote performance audit | `batch_loss_for_examples()` built graph/OD/scalar tensors, then called `predict_theta()`, which rebuilt the same tensors for the same batch. | GPU stayed underutilized and full memorization/scaling would waste substantial 5090 time on duplicate CPU preprocessing. | Reused the already-built batch tensors inside `batch_loss_for_examples()` and restarted the tmux run with the fixed code. | Addressed in code; remote rerun required. |
| G565-B011 | Remote process audit | `tmux kill-session` stopped the session wrapper but left an old G565 memorization child process alive. | Old and new runs could concurrently write the same G565 checkpoints/tables, contaminating the final artifact set. | Before relaunch, explicitly kill remaining G565 Python/solver child processes, then clean only `phase5p5_repair5g565*` artifacts/checkpoints and restart. | Addressed operationally before final remote run. |
| G565-B012 | Remote performance audit | B1/B2 matched controls built full graph/OD batches in `predict_theta()` even though B1 needs only batch size and B2 needs only leakage-free scalar features. | Matched-control memorization/scaling spent most time in irrelevant graph preparation, delaying the full 5090 run and making runtime-based decisions misleading. | Added B1/B2 fast paths and a scalar-only tensor helper; graph batches are now built only for rich actors that consume them. | Addressed in code; remote rerun required. |
| G565-B013 | Final decision audit | `write_repair5g565_decision.py` treated a failed memorization gate as incomplete evidence. | A fully executed negative result would be mislabeled `g565_incomplete_evidence_continue`, making it look like the experiment was unfinished rather than unsupported. | Split artifact completeness from gate support: complete artifacts with failed gates now produce `g565_repaired_loss_pipeline_complete_solver_transfer_not_supported`. | Addressed in code. |
| G565-B014 | Remote runtime audit | Budget-reduced evidence-completion schedules were attempted to reach fresh exact materialization quickly. | Such runs cannot fairly prove or disprove the core rich graph actor idea because short-trained checkpoints can fail for training-budget reasons. | Per user instruction, stopped the reduced run, archived it under `outputs/aborted/phase5p5_repair5g565_evidence_completion_*`, and marked it `aborted_budget_reduced_not_for_scientific_decision`. Reduced-run artifacts must not feed final gates. | Aborted and quarantined. |
| G565-B015 | Execution policy | Full G5.65 must follow the MD scientific scope: full memorization sizes, E0/E1/E2, 3 seeds, physical-map folds, rich replay retraining, fresh exact solver panel, and alpha-tail repair if needed. | Any hidden reduction in contexts, epochs, folds, seeds, model branches, replay retraining, or exact solver panel would make final conclusions scientifically ambiguous. | Only lossless accelerations are allowed: graph/OD/C0/F0 caching, B1/B2 fast path, vectorization/BF16/TF32, checkpoint/resume, and staged screening followed by full confirmatory folds/seeds. | Active policy for final run. |
| G565-B016 | Pre-full-run code audit | While adding hidden-dim screening, the memorization script trained every seed but evaluated/wrote only the last seed for each architecture. | The 3-seed memorization gate would be silently underreported and seed variance would be wrong. | Moved evaluation, row writing, gradient audit, and progress print inside the seed loop. | Addressed before full rerun. |
| G565-B017 | Lossless acceleration | `EdgeAwareAttentionLayer` used a Python loop over destination nodes for local attention softmax. | Full MD-scale epochs/folds/seeds would spend avoidable time in Python without changing model semantics. | Replaced it with scatter/segment softmax and added a parity test against the old naive loop. | Addressed and tested. |
| G565-B018 | Replay-cycle scope | Initial rich-cycle retraining script trained only one seed. | Before/after replay merge could be seed-specific and weaker than the MD's same-seed comparison intent. | Added `--seeds`, min-epoch/patience, and BF16 support so E0/E1 base/merged checkpoints can be run across 565/566/567. | Addressed before full rerun. |
| G565-B019 | Full-run BF16 audit | Segment-softmax used mixed BF16/FP32 tensors under autocast, first in denominator reduction and then in message aggregation. | Full MD run crashed at the first rich BF16 memorization batch. | Keep softmax reduction in FP32, cast final attention weights back to logits dtype, and cast messages to the node-state dtype before `index_add_`. | Addressed; full run restarted. |
| G565-B020 | Full-run staged-protocol audit | The first full MD staged run launched screening with `--folds 1`, which made every physical-map group validation-only and left the training split empty. | Screening wrote zero rows, then the staged runner silently fell back to E0/E1, so confirmatory scaling would not actually be selected from the screened rich methods. | Stopped and archived that run as `aborted_zero_row_screening_bug_not_for_scientific_decision`; added `--fold-indices`, changed screening to `--folds 4 --fold-indices 0`, and made zero-row/fallback screening fail closed. | Addressed before checkpoint-resumed full run. |
| G565-B021 | Final-decision audit | `write_repair5g565_decision.py` read `phase5p5_repair5g565_g564_truth_audit.json`, but the audit script writes `phase5p5_repair5g565_g564_truth_audit_summary.json`. | A complete full run would be mislabeled `g565_incomplete_evidence_continue` because the truth gate appeared missing. | Updated the decision writer to read the actual summary artifact; hot-patched the remote run before it reached final decision. | Addressed during full run. |
| G565-B022 | Fresh-panel coverage audit | The resume runner's fresh-solver glob covered confirmatory all-size checkpoints plus only the screening E2 all-size checkpoint. If screening selected E0/E2 or E1/E2, the non-selected rich architecture could be absent from the fresh panel. | The MD requires fresh transfer evidence for B1, B2, best E0, best E1, and best E2, so a missing rich branch would weaken solver-transfer coverage. | Updated `run_repair5g565_fresh_solver_panel.py` to auto-add screening all-size seed565 E0/E1/E2 checkpoints and fail closed if any are missing; the summary records auto-added checkpoints. | Addressed during full run before fresh panel. |
| G565-B023 | MD metric-coverage audit | Current-bank scaling rows recorded total validation risk but did not separately emit opportunity normalized regret, mixed-boundary harmful violation, trivial-context false deviation, censored trust, positive-mode capture, or theta variance. | The core scaling gate would have weaker evidence than the MD requested even if the run completed. | Added aggregate validation-detail metrics to scaling rows, wrote per-context `scaling_validation_details.csv`, and made the final decision require the confirmatory detail table. | Addressed before confirmatory scaling starts. |
| G565-B024 | Alpha-tail repair audit | The alpha response script used a non-MD alpha grid and only designed alpha rows without exact solver materialization. | If fresh transfer had tail risk, the pipeline could claim an alpha repair branch existed without real alpha-response outcomes. | Changed the default grid to `{0,0.125,0.25,0.5,0.75,1.0}`, made alpha response run exact solver materialization when fresh transfer is not positive, and made final evidence completeness require a passed alpha exact-materialization gate in that case. | Addressed before fresh solver and alpha stages. |
| G565-B025 | Expansion-branch audit | Context expansion could mark a manifest ready with fewer contexts than the target, ignored the alpha-repaired-tail trigger, and expanded training used only E0/E1 with one seed. | If the core signal was positive, Phase 9 could silently downgrade or falsely complete despite missing independent-context scale. | Expansion now fails closed when target contexts are unavailable, triggers on fresh transfer or alpha repair, final decision requires expanded-training artifacts, and expanded training defaults to E0/E1/E2 across seeds 565/566/567 with the full epoch schedule. | Addressed before expansion stage. |
| G565-B026 | Alpha-tail context selection audit | Alpha response selected quality-tail contexts by checking whether `quality_delta_vs_g556` started with a plus sign. Numeric positive deltas in CSV usually do not include `+`. | Fresh solver quality regressions without outright success regression could be omitted from the alpha-response materialization panel. | Parse `quality_delta_vs_g556` numerically and include any positive delta or success regression in the alpha-response context set. | Addressed before fresh solver and alpha stages. |
| G565-B027 | Alpha-response semantics audit | The alpha-response script blended from G556 toward an observed positive/oracle target rather than the fresh actor's raw residual. | This would test an oracle-style trust region, not the MD's required `theta(alpha)=g556+alpha*delta_actor(x)` response for the same actor residual. | Build alpha rows from `phase5p5_repair5g565_fresh_solver_panel_pairs.csv` actor theta columns, preserving raw actor method/checkpoint provenance, then materialize those alpha-scaled actor residuals exactly. | Addressed before fresh solver and alpha stages. |
| G565-B028 | Scaling-statistics gate audit | Current-bank scaling statistics lacked fold/seed variance, paired bootstrap effect intervals, and majority-fold improvement checks; the supported decision only required any rich median to improve plus win-rate > 0.5. | A confirmatory run could overstate support without proving the MD's fold/seed-stable scaling gate. | Added paired fold-seed effects, bootstrap CI, fold/seed effect variance, majority-fold checks, and made `g565_current_bank_rich_scaling_supported` require a rich method to pass the stricter scaling gate plus matched-control win rate. | Addressed before confirmatory scaling starts. |
| G565-B029 | Final-decision staged-protocol audit | The final decision writer required confirmatory scaling tables but did not require the staged-protocol summary or the screening table/summary that proves the two rich methods were selected from the one-fold screen. | A stale or manually produced ROUND-level scaling artifact could satisfy the final gate without proving the MD's staged screening -> confirmatory protocol actually completed. | Added fail-closed final checks for staged completion, no scientific downgrade, screening rows/scope, and full confirmatory folds/seeds/sizes/methods/detail rows. | Addressed during full run before final decision. |
| G565-B030 | Expanded-training scope audit | The resume runner passed `--max-contexts 1000` to expanded training even though the expansion branch targets a 5000-context manifest. | If expansion became ready, the script could train on only 1000 contexts and still report expanded training completed, silently downgrading the independent-context confirmation branch. | Expanded training now treats the manifest as authoritative: effective loading is at least the manifest size, examples are filtered by manifest UID, and missing manifest contexts fail closed. | Addressed during full run before expansion stage. |
| G565-B031 | Alpha exact-materialization hygiene audit | `run_repair5g565_alpha_response.py` defaulted to `overwrite=False`, while G549 replay infrastructure treats existing result rows as completed groups. | If old alpha-response result CSVs were present, a new alpha design could reuse stale solver rows and contaminate the tail-repair decision. | Alpha response now overwrites same-name solver outputs by default; reuse requires explicit `--resume-existing`, and the summary records the overwrite policy. | Addressed before alpha stage. |
| G565-B032 | Fresh solver exact-materialization gate audit | Fresh solver summary recorded candidate recognition, fingerprint, scenario, and identity rates but did not require them to be perfect before allowing a positive transfer decision. | A solver-transfer positive result could be reported even if actor theta materialization was incomplete or mismatched. | Fresh transfer now emits `fresh_exact_materialization_passed`, requires perfect materialization for a positive decision, and final evidence completeness also requires that flag. | Addressed before fresh solver stage. |
| G565-B033 | Fresh/alpha harmful-tail audit | Fresh transfer and alpha repair treated 鈥渘o success regression + some success gain鈥?as positive, without blocking on positive `quality_delta_vs_g556` harmful tails among both-success cases. | A rich actor could be called transfer-positive despite making successful solves lower quality on some contexts; alpha repair could also be called repaired while quality tail remained. | Fresh and alpha gates now compute worse/better counts, q90/q95/CVaR tail metrics, require zero worse-quality tail for positive/repaired decisions, and classify completed unsafe alpha as `g565_alpha_calibration_tail_remaining`. | Addressed before fresh solver and alpha stages. |
| G565-B034 | Fresh/alpha aggregate-gate audit | The harmful-tail fix initially aggregated all actor candidates and all alpha variants into one gate. | A single unsafe control/actor or unsafe alpha value could hide a genuinely safe rich actor/alpha frontier, falsely turning a supported signal into an unsupported result. | Fresh transfer now reports `per_variant_transfer` and supports only when a rich variant individually has gains with no success/quality tail; alpha repair now decides from safe gain/no-gain alpha variants rather than aggregate all-alpha rows. | Addressed before fresh solver and alpha stages. |
| G565-B035 | Fresh/alpha checkpoint-bucket audit | Per-variant gates originally grouped all checkpoints for an architecture under `E0`, `E1`, or `E2`. | A bad fold/checkpoint could hide a good checkpoint, or a good checkpoint could mask a bad one, weakening the staged/frozen-checkpoint interpretation. | Fresh and alpha summaries now bucket by architecture plus `model_path`/method, so support and tail risk are attributed at checkpoint/alpha-checkpoint granularity. | Addressed before fresh solver and alpha stages. |
| G565-B036 | Alpha calibration scope audit | The G5.65 alpha script materializes an exact alpha-response panel, but it does not train an `alpha_hat(x)` trust head or rerun a fresh fixed solver panel with a calibrated actor. | Treating alpha-response success alone as 鈥渢rust calibration repairs tail risk鈥?could incorrectly trigger independent-context expansion or imply a deployment-ready calibrated actor. | Alpha summaries now explicitly mark `alpha_trust_head_trained=false` and `fresh_fixed_panel_reran_after_alpha=false`; context expansion can only use an alpha repair trigger if those full calibration fields are true. | Addressed before alpha and expansion stages. |
| G565-B037 | Fresh checkpoint provenance audit | Fresh solver checkpoint discovery used filesystem globs over `phase5p5_repair5g565_scaling_*_all_seed565.pt`. | Stale checkpoints from aborted/reduced/older runs could be picked up if they shared the prefix and were not overwritten by the current confirmatory stage. | Fresh solver now filters checkpoints through current manifest evidence: confirmatory scaling table all-size seed565 rows, required screening E0/E1/E2 all-size checkpoints, and rich-cycle merged seed565 rows; non-manifest checkpoint paths are rejected and recorded. | Addressed before fresh solver stage. |
| G565-B038 | Fresh panel split-scope audit | Fresh solver defaulted to `preferred_split=any`, and the runner did not override it. | The fresh transfer panel could include training contexts, contradicting the MD requirement for heldout/new evidence and potentially inflating transfer claims. | Fresh solver now defaults to `validation,heldout,blind,test`, fails closed below 400 preferred contexts, records split counts/train-context count, and final evidence completeness requires at least 400 fresh contexts with zero train contexts. | Addressed before fresh solver stage. |
| G565-B039 | Context-expansion trigger audit | Expansion trigger was implemented as `scaling_supported AND (fresh_positive OR alpha_repaired)`. | This was stricter than the MD, which says expansion should proceed if any one of valid scaling, fresh larger-data transfer, or full alpha repair is positive; a scaling-supported result could incorrectly skip expansion. | Context expansion now uses `scaling_supported OR fresh_transfer_positive OR fully_calibrated_alpha_repair` and records the trigger rule. | Addressed before expansion stage. |
| G565-B040 | Label-margin freshness audit | Label/noise margin calibration ran before the G5.65 fresh exact solver panel, even though the calibration script can include `phase5p5_repair5g565_fresh_solver_panel_pairs.csv`. | Final evidence could treat a margin estimated from old G5.62 rows as satisfying the MD requirement for a nonzero margin from fresh paired diagnostic rows. | Fresh solver now reruns `calibrate_repair5g565_label_margin.py` after writing fresh pairs; final evidence completeness requires both fresh-pair inclusion in the margin summary and a fresh-summary flag proving the recalibration happened after fresh materialization. | Addressed before fresh solver stage. |
| G565-B041 | Dual-channel materialization gate audit | Fresh and alpha exact-materialization gates checked recognition/fingerprint/scenario/identity but did not require `force_additive=false` and `dual_channel_enabled=true` for learned actor rows. | A row could pass the transfer gate while not actually executing the intended learned dual-channel UpdateParams path. | Fresh and alpha summaries now record `force_additive_false_rate` and `dual_channel_enabled_rate`; exact materialization passes only when both are 1.0 for all actor rows. | Addressed before fresh solver and alpha stages. |
| G565-B042 | Rich-cycle evidence-detail audit | Rich replay retraining rows recorded only aggregate validation risk, while the MD requires post-merge validation opportunity regret to be reported. | A before/after replay merge could appear complete without proving whether opportunity contexts improved or regressed under the canonical repaired-loss decomposition. | Rich-cycle retraining now aggregates opportunity regret, mixed-boundary harmful violation, trivial false deviation, censored trust, positive capture, and writes per-context validation details; final evidence completeness requires post-merge opportunity regret and the detail table. | Addressed before rich-cycle stage. |
| G565-B043 | Fresh solver overwrite hygiene audit | Fresh solver relied on the caller to pass `--overwrite`; the resume runner did, but the script default could still reuse stale result rows in direct use. | A future direct fresh-panel run could mark stale solver groups as completed and contaminate transfer evidence. | Fresh solver now overwrites same-name solver outputs by default; reusing existing rows requires explicit `--resume-existing`, and summaries record `solver_overwrite`. | Addressed before fresh solver stage. |
| G565-B044 | Alpha-response scope audit | The resume runner invoked alpha response with `--max-contexts 256`, below the MD's 400-800 fresh/alpha substantive-evidence scale. | If fresh transfer was tail-blocked, alpha repair could materialize too few tail contexts and be wrongly interpreted as a full tail repair/no-gain result. | `run_repair5g565_alpha_response.py` now enforces an 800-context effective minimum even when an older runner passes 256, and records requested/effective context limits in the summary. | Addressed before alpha stage. |
| G565-B045 | Remote performance audit | `train_model()` computed the gradient-summary audit after every training batch, forcing repeated parameter traversal and CPU/GPU synchronization. | Full confirmatory scaling and rich-cycle retraining spent avoidable time on reporting-only work, slowing the 5090 run without improving scientific evidence. | Gradient summary is now sampled once per model training run after the first backward pass; optimization, epochs, seeds, folds, methods, and contexts are unchanged. | Addressed before confirmatory scaling starts. |
| G565-B046 | Rich-cycle architecture-scope audit | `train_repair5g565_rich_cycle.py` hardcoded replay retraining to `E0,E1`, while the staged screen can select `E2` and the MD requires rich replay retraining without dropping rich branches. | The post-merge rich-cycle evidence and fresh merged-checkpoint panel could omit a selected/top rich actor, especially when confirmatory selects `E1,E2`. | Rich-cycle retraining now accepts `--architectures` and defaults to `E0,E1,E2`, recording the architecture list in the summary. | Addressed before rich-cycle stage. |
| G565-B047 | Context-expansion diversity audit | `generate_repair5g565_context_expansion.py` only checked the number of manifest rows and did not enforce the MD's minimum 32 physical-map hashes and 10 map/morphology families. | A large but under-diverse expansion bank could be marked ready, creating a false independent-context confirmation. | Added a fail-closed `expansion_gate()` requiring target contexts, physical-map diversity, and family diversity; the summary records block reasons. | Addressed before expansion stage. |
| G565-B048 | Expanded-panel completion audit | The pipeline could finish expanded training after a triggered expansion without rerunning the same fixed exact solver panel on expanded checkpoints. | Phase 9 could be treated as complete without measuring whether the larger-data rich actors transfer in the solver. | Added `run_repair5g565_expanded_solver_panel.py` and made final evidence completeness require an exact expanded solver panel with nontrain scope and at least 5000 actor diagnostic rows whenever expansion is ready. | Addressed before expansion stage; remote runner must call the new script before final decision. |
| G565-B049 | Remote dependency audit | The 5090 working tree was missing `scripts/run_repair5g562_replay_truth.py`, which is imported by the G5.65 fresh, alpha, and expanded exact solver panels. | The full run would have reached fresh transfer and failed before producing solver evidence, despite successful scaling/rich-cycle stages. | Uploaded the missing helper to `/root/czr004`, verified imports for G562 replay truth, fresh solver, and expanded solver, and reran the remote transfer-gate pytest shard. | Addressed before fresh solver stage. |
| G565-B050 | Expansion data-generation audit | The existing G559/G560 real-label bank generator was capped at the original 24 default topologies and about 8 families. | A triggered G5.65 expansion could not satisfy the MD diversity floor of at least 32 physical-map hashes and 10 map/morphology families without a separate top-up path. | Added a backward-compatible `--topology-count` option and extended the synthetic topology list to 40 maps / 16 families so a no-downgrade expansion top-up can generate the required independent-context diversity. | Addressed before expansion top-up. |
| G565-B051 | Expansion balance-gate audit | The expansion manifest gate did not check agent-count or budget-tier coverage even though the MD requires balanced density, budget, agent count, and OD regimes. | A 5000-context expansion could still be narrow in solver budget or agent scale, weakening the independent-context claim. | Expansion now fail-closes unless the manifest has at least four agent-count tiers and four budget tiers; it also records available OD-regime metadata when present. | Addressed before expansion stage. |
| G565-B052 | Overnight execution audit | The main G5.65 runner would stop after writing an incomplete final decision if expansion was triggered but the existing 2000-context bank could not satisfy the 5000-context/diversity gate. | The 5090 server could sit idle overnight even though the next no-downgrade action is known: build the larger independent-context bank and continue Phase 8/9. | Patched the remote tmux runner to detect `g565_context_expansion_blocked_incomplete_scope`, archive the pre-expansion bank, run a 5000-context/40-topology G559 real-label top-up, recover G560 Label-v5.1 rows, rerun expansion, then continue expanded training, expanded solver panel, and final decision. | Addressed in the active remote runner before expansion stage. |
| G565-B053 | Tmux runner parse audit | The active bash runner may already have parsed its `{ ... } | tee` compound command before the B048/B052 command insertions were written to disk. | File-level patches to the runner might not be executed by the already-running shell, so the server could still stop after the older command list. | Added an independent `g565_tail` tmux controller that waits for the main runner to exit, then inspects artifacts and runs missing fresh/alpha/expansion/top-up/expanded-panel/final steps without reducing scientific scope. | Addressed before the main runner reached expansion. |
| G565-B054 | Final missing-artifact audit | The final decision gate read `g564_truth_audit_summary.json` and `loss_ablation_summary.json`, but those JSON files were not listed in `required_artifacts()`. | If either summary was missing, the run would be incomplete while the missing-artifact list pointed at less useful neighboring files. | Added both summary JSON files to the required-artifact list so final failures identify the actual missing gate evidence. | Addressed before final decision. |
| G565-B055 | Remote runner EOF audit | The active resume script reached the post-alpha expansion decision, wrote `g565_incomplete_evidence_continue`, then exited with `unexpected EOF while looking for matching \`'` from the patched shell wrapper. | The shell error occurred after fresh, alpha, expansion, and incomplete-decision artifacts were written, but it prevented any in-script top-up/expanded-panel continuation from running. | Treated the written artifacts as valid stage evidence, left them intact, and relied on the independent continuation controller to run the missing no-downgrade expansion top-up and Phase 9 artifacts. | Addressed operationally during remote run; script quoting should be cleaned before reuse. |
| G565-B056 | Tmux continuation liveness audit | `g565_tail` detected main-runner liveness by grepping for `run_g565_resume_after_mem.sh`; after the dead `g565` pane was removed, the tmux server process still kept that old command line. | The continuation controller could wait forever even though the main runner was dead and expansion top-up was required. | Restarted the controller as `g565_tail2` under a clean tmux server command line; it immediately audited artifacts, detected expansion scope blocking, and launched the 5000-context/40-topology top-up. | Addressed operationally during remote run; future liveness checks should ignore tmux server wrapper command lines. |
| G565-B057 | Remote metadata/log audit | The G559 expansion top-up emits repeated `fatal: not a git repository` messages while launching solver tasks from remote artifact paths. | The messages clutter watchdog/error scans and could be mistaken for a scientific failure, although solver tasks continue and write real pair rows. | Treat the messages as metadata-provenance noise for this run, verify row growth and exact materialization instead, and clean the git-metadata helper/cwd handling before reusing the top-up launcher. | Observed during expansion top-up; not blocking current run. |
| G565-B058 | Expansion top-up interpretation audit | The no-downgrade 5000-context/40-topology top-up satisfied G5.65 context diversity, but the underlying G559/G560 summaries still reported G559 learnability failure and phase5p5_allowed=false/runtime claims disallowed. | The expanded context bank can support G5.65 diversity and exact-materialized actor diagnostics, but it must not be cited as a successful G559 runtime-policy result. | Keep the expansion evidence scoped to G5.65 independent-context coverage; final reporting must preserve the failed G559/G560 runtime gates and avoid any runtime-policy success claim from the top-up. | Observed during expansion top-up; final report must carry this limitation. |

## Ongoing Rule

If another bug, hidden assumption, or weak gate is found during local smoke, remote tmux execution, artifact pullback, or final audit, append it here before committing and pushing G5.65.
